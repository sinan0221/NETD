<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Department Logo</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/cform.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-7">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">Upload Department Logo</h5>
                    </div>
                    <div class="card-body">
                        
                        {{#if success}}
                        <div class="alert alert-success" role="alert">
                            Department Logo added/updated successfully!
                        </div>
                        {{/if}}
                        
                        {{#if error}}
                        <div class="alert alert-danger" role="alert">
                            Error: {{error}}
                        </div>
                        {{/if}}

                        <form action="/admin/add-deplogo" method="POST" enctype="multipart/form-data">
                        

                          

                            <div class="form-group mb-3">
                                <label for="centreId" class="form-label">Centre ID (Linked to Department)</label>
                                <input type="text" 
                                       class="form-control" 
                                       id="centreId" 
                                       name="centreId" 
                                       placeholder="Enter the Centre ID" 
                                       required>
                                <small class="form-text text-muted">The logo will be linked to this specific centre.</small>
                            </div>

                            <div class="form-group mb-3">
                                <label for="department" class="form-label">Select Department</label>
                                <select id="department" name="department" class="form-control" required>
                               {{#if department}}
      <option value="{{department}}" selected>{{department}}</option>
    {{else}}
      <option value="">Select Department</option>
    {{/if}}
                                    <option value="Department of paramedical education">Department of Paramedical Education</option>
                                    <option value="mobile phone technology">Mobile phone technology</option>
                                    </select>
                                <small class="form-text text-muted">This Department Name will be used for database lookup (e.g., in future features).</small>
                            </div>
                            
                            <div class="form-group mb-4">
                                <label for="deptLogo" class="form-label">Upload Department Logo (.jpg, .png)</label>
                                <input type="file" 
                                       class="form-control" 
                                       id="deptLogo" 
                                       name="deptLogo" 
                                       accept="image/jpeg, image/png" 
                                       required>
                                <small class="form-text text-muted">This image will be stored as the department logo for the selected Centre ID.</small>
                            </div>
                            
                            <button type="submit" class="btn btn-primary mt-3">Upload Department Logo</button>
                            <a href="/admin" class="btn btn-secondary mt-3">Cancel</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>