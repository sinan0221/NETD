<style>
/* ============================================
   FORM CONTAINER
============================================ */
.form-container {
  background: linear-gradient(145deg, #1e2b47 0%, #0f1a30 100%);
  border-radius: 16px;
  padding: 30px;
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.25);
  border: 1px solid rgba(255, 215, 0, 0.1);
  max-width: 900px;
  margin: 0 auto;
}

/* ============================================
   FORM HEADER
============================================ */
.form-header {
  text-align: center;
  margin-bottom: 35px;
  padding-bottom: 20px;
  border-bottom: 1px solid rgba(255, 215, 0, 0.2);
}

.form-title {
  color: #ffd700; /* Golden brown */
  font-size: 1.75rem;
  font-weight: 700;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
}

.form-subtitle {
  color: #a0b0d0;
  font-size: 0.95rem;
  opacity: 0.9;
  margin: 0;
}

/* ============================================
   FORM SECTIONS
============================================ */
.form-section {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 25px;
  margin-bottom: 20px;
  border-left: 4px solid #ffd700;
  transition: all 0.3s ease;
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.form-section:hover {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 215, 0, 0.3);
  transform: translateY(-2px);
}

.section-header {
  display: flex;
  align-items: center;
  gap: 20px;
  margin-bottom: 20px;
}

.section-icon {
  background: linear-gradient(135deg, #ffd700 0%, #daa520 100%);
  width: 50px;
  height: 50px;
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.section-icon i {
  color: #1e2b47;
  font-size: 1.5rem;
}

.section-title {
  flex: 1;
}

.section-name {
  color: #e6f2ff;
  font-weight: 600;
  font-size: 1.2rem;
  margin-bottom: 5px;
}

.section-description {
  color: #a0b0d0;
  font-size: 0.9rem;
}

/* ============================================
   FORM GROUPS
============================================ */
.form-group {
  margin-bottom: 20px;
}

.form-group:last-child {
  margin-bottom: 0;
}

.form-row {
  display: flex;
  gap: 15px;
  margin-bottom: 15px;
}

.form-row .input-group {
  flex: 1;
  margin-bottom: 0;
}

.form-label {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 10px;
}

.label-text {
  color: #e6f2ff;
  font-weight: 600;
  font-size: 1.05rem;
}

.label-required {
  color: #ff6b6b;
  font-weight: bold;
}

.label-optional {
  color: #a0b0d0;
  font-size: 0.85rem;
  font-style: italic;
}

/* ============================================
   INPUT STYLES
============================================ */
.input-group {
  position: relative;
  margin-bottom: 8px;
}

.input-group-text {
  background: linear-gradient(135deg, #2d3b5e 0%, #1e2b47 100%);
  border: 1px solid rgba(255, 215, 0, 0.2);
  border-right: none;
  color: #ffd700;
  font-size: 1rem;
  padding: 12px 15px;
  min-width: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.form-control {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 215, 0, 0.2);
  border-left: none;
  color: #ffffff;
  padding: 12px 15px;
  font-size: 1rem;
  height: 48px;
  transition: all 0.3s ease;
  width: 100%;
}

.form-control:focus {
  background: rgba(255, 255, 255, 0.08);
  border-color: #ffd700;
  box-shadow: 0 0 0 0.2rem rgba(255, 215, 0, 0.15);
  color: #ffffff;
  outline: none;
}

.form-control::placeholder {
  color: rgba(160, 176, 208, 0.6);
}

/* ============================================
   FORM HINTS
============================================ */
.form-hint {
  color: #a0b0d0;
  font-size: 0.85rem;
  margin-top: 8px;
  display: flex;
  align-items: center;
  gap: 5px;
  padding-left: 5px;
}

.form-hint i {
  color: #ffd700;
  font-size: 0.8rem;
}

/* ============================================
   FORM ACTIONS
============================================ */
.form-actions {
  display: flex;
  gap: 15px;
  margin-top: 30px;
  padding-top: 25px;
  border-top: 1px solid rgba(255, 215, 0, 0.1);
}

.btn-submit {
  background: linear-gradient(135deg, #ffd700 0%, #daa520 100%);
  color: #1e2b47;
  border: none;
  padding: 14px 30px;
  font-size: 1.05rem;
  font-weight: 600;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  flex: 1;
}

.btn-submit:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(255, 215, 0, 0.25);
  color: #1e2b47;
}

.btn-submit:active {
  transform: translateY(0);
}

.btn-reset {
  background: transparent;
  color: #a0b0d0;
  border: 1px solid rgba(160, 176, 208, 0.3);
  padding: 14px 25px;
  font-size: 1rem;
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  flex: 0.5;
}

.btn-reset:hover {
  background: rgba(160, 176, 208, 0.1);
  color: #ffffff;
  border-color: rgba(255, 215, 0, 0.3);
}

/* ============================================
   FORM FOOTER
============================================ */
.form-footer {
  margin-top: 25px;
  text-align: center;
  padding-top: 20px;
  border-top: 1px solid rgba(255, 215, 0, 0.05);
}

.footer-note {
  color: #a0b0d0;
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  opacity: 0.8;
}

.footer-note i {
  color: #ffd700;
  font-size: 0.9rem;
}

/* ============================================
   RESPONSIVE DESIGN
============================================ */
@media (max-width: 768px) {
  .form-container {
    padding: 20px;
    margin: 10px;
  }

  .form-title {
    font-size: 1.5rem;
    flex-direction: column;
    gap: 5px;
  }

  .form-section {
    padding: 20px;
  }

  .section-header {
    flex-direction: column;
    text-align: center;
    gap: 15px;
  }

  .section-icon {
    align-self: center;
  }

  .form-row {
    flex-direction: column;
    gap: 10px;
  }

  .form-actions {
    flex-direction: column;
  }

  .btn-submit,
  .btn-reset {
    width: 100%;
  }
}

/* ============================================
   VALIDATION STATES
============================================ */
.form-control:valid {
  border-color: rgba(76, 175, 80, 0.3);
}

.form-control:invalid:not(:placeholder-shown) {
  border-color: rgba(244, 67, 54, 0.3);
}

.form-control:invalid:not(:placeholder-shown) + .form-hint {
  color: #ff6b6b;
}

/* ============================================
   ANIMATIONS
============================================ */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.links-form {
  animation: fadeIn 0.5s ease-out;
}
</style>

<!-- Professional Links Form with Dark Blue & Golden Brown Theme -->
<div class="form-container">
  <form method="POST" class="links-form">
    <!-- Form Header -->
    <div class="form-header">
      <h3 class="form-title">
        <i class="fas fa-link me-2"></i>
        Batch Links Configuration
      </h3>
      <p class="form-subtitle">Configure important URLs for this batch</p>
    </div>

    <!-- Payment Link Section -->
    <div class="form-section payment-section">
      <div class="section-header">
        <div class="section-icon">
          <i class="fas fa-credit-card"></i>
        </div>
        <div class="section-title">
          <div class="section-name">Payment Link</div>
          <div class="section-description">Students will use this link to make payments</div>
        </div>
      </div>
      <div class="section-content">
        <div class="form-group">
          <div class="input-group">
            <span class="input-group-text">
              <i class="fas fa-dollar-sign"></i>
            </span>
            <input 
              type="url" 
              name="paymentLink"
              value="{{batch.paymentLink}}"
              class="form-control"
              placeholder="https://payment.example.com/pay/..."
              pattern="https://.*"
              required
            >
          </div>
          <div class="form-hint">
            <i class="fas fa-info-circle me-1"></i>
            Required: Students will use this link to make payments
          </div>
        </div>
      </div>
    </div>

    <!-- Course Link 1 Section -->
    <div class="form-section course-section">
      <div class="section-header">
        <div class="section-icon">
          <i class="fas fa-graduation-cap"></i>
        </div>
        <div class="section-title">
          <div class="section-name">Online Course 1</div>
          <div class="section-description">Primary online learning platform or course materials</div>
        </div>
      </div>
      <div class="section-content">
        <div class="form-group">
          <div class="form-row">
            <div class="input-group">
              <span class="input-group-text">
                <i class="fas fa-book"></i>
              </span>
              <input 
                type="text" 
                name="courseName1"
                value="{{batch.courseName1}}"
                class="form-control"
                placeholder="Course Name (e.g., Digital Marketing Fundamentals)"
              >
            </div>
          </div>
          <div class="form-row">
            <div class="input-group">
              <span class="input-group-text">
                <i class="fas fa-link"></i>
              </span>
              <input 
                type="url" 
                name="courseLink1"
                value="{{batch.courseLink1}}"
                class="form-control"
                placeholder="https://learning-platform.com/course/..."
                pattern="https://.*"
              >
            </div>
          </div>
          <div class="form-hint">
            <i class="fas fa-info-circle me-1"></i>
            Optional: Enter course name and link for primary online learning platform
          </div>
        </div>
      </div>
    </div>

    <!-- Course Link 2 Section -->
    <div class="form-section course-section">
      <div class="section-header">
        <div class="section-icon">
          <i class="fas fa-book-open"></i>
        </div>
        <div class="section-title">
          <div class="section-name">Online Course 2</div>
          <div class="section-description">Secondary learning resources or additional course materials</div>
        </div>
      </div>
      <div class="section-content">
        <div class="form-group">
          <div class="form-row">
            <div class="input-group">
              <span class="input-group-text">
                <i class="fas fa-book"></i>
              </span>
              <input 
                type="text" 
                name="courseName2"
                value="{{batch.courseName2}}"
                class="form-control"
                placeholder="Course Name (e.g., Advanced Programming Techniques)"
              >
            </div>
          </div>
          <div class="form-row">
            <div class="input-group">
              <span class="input-group-text">
                <i class="fas fa-link"></i>
              </span>
              <input 
                type="url" 
                name="courseLink2"
                value="{{batch.courseLink2}}"
                class="form-control"
                placeholder="https://another-platform.com/course/..."
                pattern="https://.*"
              >
            </div>
          </div>
          <div class="form-hint">
            <i class="fas fa-info-circle me-1"></i>
            Optional: Enter course name and link for secondary learning resources
          </div>
        </div>
      </div>
    </div>

    <!-- Form Actions -->
    <div class="form-actions">
      <button type="submit" class="btn-submit">
        <i class="fas fa-save me-2"></i>
        Save Links
      </button>
      <button type="reset" class="btn-reset">
        <i class="fas fa-redo me-2"></i>
        Reset
      </button>
    </div>

    <!-- Form Footer -->
    <div class="form-footer">
      <div class="footer-note">
        <i class="fas fa-shield-alt me-2"></i>
        All links are secured with HTTPS encryption
      </div>
    </div>
  </form>
</div>

<script>
// Add some interactivity
document.addEventListener('DOMContentLoaded', function() {
  const form = document.querySelector('.links-form');
  const inputs = form.querySelectorAll('input[type="url"], input[type="text"]');
  
  // Add focus effect
  inputs.forEach(input => {
    input.addEventListener('focus', function() {
      this.parentElement.parentElement.parentElement.parentElement.classList.add('focused');
    });
    
    input.addEventListener('blur', function() {
      this.parentElement.parentElement.parentElement.parentElement.classList.remove('focused');
    });
  });
  
  // Form submission animation
  form.addEventListener('submit', function(e) {
    e.preventDefault();
    
    const submitBtn = this.querySelector('.btn-submit');
    const originalText = submitBtn.innerHTML;
    
    submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>Saving...';
    submitBtn.disabled = true;
    
    // Submit the form after animation
    setTimeout(() => {
      this.submit();
    }, 1500);
  });
});
</script>