<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Verification | NETD</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-blue: #2c3e50;
            --secondary-blue: #3498db;
            --accent-blue: #2980b9;
            --light-blue: #ecf0f1;
            --success-green: #27ae60;
            --warning-orange: #f39c12;
            --danger-red: #e74c3c;
            --dark-gray: #2c3e50;
            --light-gray: #f8f9fa;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #cbd1e3  100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .login-container {
            max-width: 480px;
            margin: 80px auto;
            padding: 0 20px;
        }

        .login-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(44, 62, 80, 0.15);
            padding: 40px;
            position: relative;
            overflow: hidden;
            border: none;
        }

        .login-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, var(--secondary-blue), var(--accent-blue));
        }

        .login-header {
            text-align: center;
            margin-bottom: 35px;
        }

        .login-header h2 {
            color: var(--primary-blue);
            font-weight: 700;
            font-size: 1.5rem;
            margin-bottom: 10px;
            position: relative;
            display: inline-block;
        }

        .login-header h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: linear-gradient(90deg, var(--secondary-blue), var(--accent-blue));
            border-radius: 3px;
        }

        .login-header p {
            color: #7f8c8d;
            font-size: 1.0rem;
            margin-top: 20px;
        }

        .form-label {
            font-weight: 600;
            color: var(--dark-gray);
            margin-bottom: 8px;
            font-size: .9rem;
        }

        .form-control {
            padding: 14px 18px;
            border: 2px solid #dfe6e9;
            border-radius: 12px;
            font-size: .9rem;
            transition: all 0.3s ease;
            background-color: var(--light-gray);
        }

        .form-control:focus {
            border-color: var(--secondary-blue);
            box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
            background-color: white;
        }

        .form-control::placeholder {
            color: #95a5a6;
        }

        .input-group-text {
            background-color: var(--light-blue);
            border: 2px solid #dfe6e9;
            border-right: none;
            color: var(--secondary-blue);
        }

        .alert-danger {
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 15px 20px;
            margin-bottom: 25px;
            font-weight: 500;
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.2);
        }

        .alert-danger i {
            margin-right: 10px;
            font-size: 1.2rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--secondary-blue), var(--accent-blue));
            border: none;
            border-radius: 12px;
            padding: 16px;
            font-weight: 600;
            font-size: 1.1rem;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(52, 152, 219, 0.4);
        }

        .btn-primary:active {
            transform: translateY(0);
        }

        .login-footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 25px;
            border-top: 1px solid #eee;
            color: #7f8c8d;
            font-size: 0.95rem;
        }

        .login-footer a {
            color: var(--secondary-blue);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .login-footer a:hover {
            color: var(--accent-blue);
            text-decoration: underline;
        }

        .institution-badge {
            display: inline-block;
            background: var(--light-blue);
            color: var(--primary-blue);
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            margin-top: 15px;
            border: 1px solid #bdc3c7;
        }

        .input-icon {
            position: relative;
        }

        .input-icon .form-control {
            padding-left: 50px;
        }

        .input-icon i {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--secondary-blue);
            font-size: 1.2rem;
            z-index: 10;
        }

        /* Animation for form */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .login-card {
            animation: fadeInUp 0.5s ease-out;
        }

        /* Responsive adjustments */
        @media (max-width: 576px) {
            .login-container {
                margin: 40px auto;
                padding: 0 15px;
            }
            
            .login-card {
                padding: 30px 25px;
            }
            
            .login-header h2 {
                font-size: 1.8rem;
            }
        }

        /* Loading animation for button */
        .btn-loading {
            position: relative;
            color: transparent !important;
        }

        .btn-loading::after {
            content: '';
            position: absolute;
            width: 20px;
            height: 20px;
            top: 50%;
            left: 50%;
            margin-left: -10px;
            margin-top: -10px;
            border: 3px solid rgba(255,255,255,0.3);
            border-radius: 50%;
            border-top-color: white;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <h2><i class="fas fa-user-graduate me-2"></i>Student <br>Verification</h2>
                <p class="mt-3">Enter your credentials to access your academic records</p>
                
            </div>

            {{#if loginErr}}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle"></i>
                {{loginErr}}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            {{/if}}

            <form action="/student/student-access" method="POST" id="verificationForm">
                <div class="mb-4">
                    <label class="form-label">
                        <i class="fas fa-id-card me-2"></i>Registration Number
                    </label>
                    <div class="input-icon">
                        <i class="fas fa-hashtag"></i>
                        <input 
                            type="text" 
                            name="regNo" 
                            required 
                            class="form-control" 
                            placeholder="Enter your registration number"
                            autocomplete="off"
                        >
                    </div>
                    <div class="form-text mt-2">
                        <small><i class="fas fa-info-circle me-1"></i>Enter the registration number provided by your institution</small>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label">
                        <i class="fas fa-calendar-alt me-2"></i>Date of Birth
                    </label>
                    <div class="input-icon">
                        <i class="fas fa-birthday-cake"></i>
                        <input 
                            type="date" 
                            name="dob" 
                            required 
                            class="form-control"
                            max="{{currentDate}}"
                        >
                    </div>
                    <div class="form-text mt-2">
                        <small><i class="fas fa-info-circle me-1"></i>Format: DD/MM/YYYY</small>
                    </div>
                </div>

                <button type="submit" class="btn btn-primary w-100" id="submitBtn">
                    <i class="fas fa-sign-in-alt me-2"></i>Verify & Access Records
                </button>
            </form>

            <div class="login-footer mt-4">
                <p class="mb-2">
                    <i class="fas fa-shield-alt me-2"></i>Your information is securely encrypted
                </p>
                
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Set max date for DOB field to today
        document.addEventListener('DOMContentLoaded', function() {
            const today = new Date().toISOString().split('T')[0];
            document.querySelector('input[name="dob"]').setAttribute('max', today);
            
            // Form submission loading state
            const form = document.getElementById('verificationForm');
            const submitBtn = document.getElementById('submitBtn');
            
            form.addEventListener('submit', function() {
                submitBtn.classList.add('btn-loading');
                submitBtn.disabled = true;
            });
            
            // Clear loading state if form has error
            if (document.querySelector('.alert-danger')) {
                submitBtn.classList.remove('btn-loading');
                submitBtn.disabled = false;
            }
        });
    </script>
</body>
</html>